apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test
spec:
  # ceph-block is the default StorageClass
  # storageClassName: ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
